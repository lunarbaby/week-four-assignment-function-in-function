'use strict;'

//Function one
//set variable to a string, console.log variable, call next function

//Function two
//set variable to a string, console log new string, call next function with variable as a parameter

//Function three
//console.log variable, set in function the number 1 added to the end of the string, call next function with variable as a parameter.

//Function four
//make function that checks to see if argument has less than 5 characters long, if true console.log true else console.log false, and call next function with the variable as a parameter.

//Function five
//Create a loop that runs as many times as the length of the string and each run console.log a string